user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';

    access_log /var/log/nginx/access.log main;

    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    client_max_body_size 20M;

    # Upstream para usuario-service con balanceo por menos conexiones
    upstream usuario_servers {
        least_conn;
        server usuario-service-1:8090 weight=1 max_fails=3 fail_timeout=30s;
        server usuario-service-2:8090 weight=1 max_fails=3 fail_timeout=30s;
    }

    # Balanceador 2 para Usuario (Redundancia)
    server {
        listen 9090;
        server_name _;

        location / {
            proxy_pass http://usuario_servers;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection 'upgrade';
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_cache_bypass $http_upgrade;
            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;
        }

        location /health {
            access_log off;
            return 200 "Usuario LB-2 Healthy\n";
            add_header Content-Type text/plain;
        }
    }

    # Stats endpoint
    server {
        listen 8401;
        server_name _;
        
        location /stats {
            stub_status on;
            access_log off;
            allow 127.0.0.1;
            allow all;
        }
    }
}
